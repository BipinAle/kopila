<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mobile.kopila">
     <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MapActivity"></activity>
        <activity android:name=".activities.PhotoGalleryActivity" />
        <activity android:name=".activities.JankariSuchanaActivity" />
        <activity android:name=".activities.PhoneDiaryActivity" />
        <activity android:name=".activities.DaunghaKoChinari" />
        <activity android:name=".activities.HamroBaremaActivity" />
        <activity android:name=".activities.DashboardActivity" />
        <activity android:name=".activities.PersonListActivity" />
        <activity android:name=".activities.SahyogSamparkaActivity" />
        <activity android:name=".activities.LekhharuActivity"/>
        <activity android:name=".activities.AgreementActivity">


            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>